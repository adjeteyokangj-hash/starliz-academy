# Short Learning UAT summary

- Base: http://localhost:3000
- Capture mode: HTML + API
- Passed: **36/36**
- Failed: **0**
- Server still up: true
- Evidence location (current): `artifacts/uat/short-learning/` (regenerate with `npm run uat:short-learning`)
- Historical captures (obsolete path): `scripts/uat-short-learning-evidence/`

## Failures

- none

## Notes

Permanent audit record of the pre-launch Short Learning authenticated UAT. Bulky HTML dumps remain under historical `scripts/` paths until tree-trim; new runs write to `artifacts/` (gitignored).
